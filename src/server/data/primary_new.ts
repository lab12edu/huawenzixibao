// ============================================================
// primary_new.ts — P1new & P2new New-Syllabus vocabulary
// Server-side only — never bundled into the frontend.
//
// Re-exports the existing per-semester data files so the vault
// can reference each semester array by name.
// ============================================================

// ── P1 New Syllabus ───────────────────────────────────────────
export { P1newA } from '../../data/vocab_P1newA'
export { P1newB } from '../../data/vocab_P1newB'

// ── P2 New Syllabus ───────────────────────────────────────────
export { P2newA } from '../../data/vocab_P2newA'
export { P2newB } from '../../data/vocab_P2newB'
